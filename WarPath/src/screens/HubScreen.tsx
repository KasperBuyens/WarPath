import { View, Text } from 'react-native';

// Hub screen: list/carousel of the player's tribes with delete option, "ATTACK!" (→ Map)
// and "New Tribe" (→ CreateTribe) buttons.

export default function HubScreen() {
  return (
    <View>
      <Text>Hub</Text>
    </View>
  );
}
