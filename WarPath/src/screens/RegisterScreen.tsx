import { View, Text } from 'react-native';

// Register screen: username + password + confirm password, Register button. On success → Hub.

export default function RegisterScreen() {
  return (
    <View>
      <Text>Register</Text>
    </View>
  );
}
