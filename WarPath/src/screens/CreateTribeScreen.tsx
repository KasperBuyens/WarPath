import { View, Text } from 'react-native';

// Create Tribe screen: tribe name input, leader carousel (melee / ranged / magic),
// shows leader bonus, Create button → back to Hub.

export default function CreateTribeScreen() {
  return (
    <View>
      <Text>Create Tribe</Text>
    </View>
  );
}
