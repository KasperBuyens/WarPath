import { View, Text } from 'react-native';

// Login screen: username + password fields, Login button. On success → Hub.

export default function LoginScreen() {
  return (
    <View>
      <Text>Login</Text>
    </View>
  );
}
