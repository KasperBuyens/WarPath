import { View, Text } from 'react-native';

// Map screen: scrollable map showing the 3 battle locations connected by a path.
// Locked locations are grey until prerequisites are conquered. Bottom nav links to
// TribeStats, TrainOrcs, and back to Hub. Tapping an unlocked location → Battle.

export default function MapScreen() {
  return (
    <View>
      <Text>Map</Text>
    </View>
  );
}
