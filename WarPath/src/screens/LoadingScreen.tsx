import { View, Text } from 'react-native';

// Loading / splash screen shown while app boots (orc artwork from the design).

export default function LoadingScreen() {
  return (
    <View>
      <Text>Loading</Text>
    </View>
  );
}
