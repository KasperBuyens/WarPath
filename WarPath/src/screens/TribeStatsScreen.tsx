import { View, Text } from 'react-native';

// Tribe Stats screen: name, leader + bonus info, melee/ranged counts, conquered locations.
// Bottom nav back to Map / TrainOrcs.

export default function TribeStatsScreen() {
  return (
    <View>
      <Text>Tribe Stats</Text>
    </View>
  );
}
