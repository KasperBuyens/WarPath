import { View, Text } from 'react-native';

// Train Orcs screen: minigame — tap targets to train ranged units, tap the anvil
// to forge melee swords. Includes an "activate cheats" button for demo speed.

export default function TrainOrcsScreen() {
  return (
    <View>
      <Text>Train Orcs</Text>
    </View>
  );
}
